name: CI/CD (Build, Push, Deploy)

on:
  push:
    branches: ["**"]
  workflow_dispatch: {}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: backend
            context: backend
            image: docker.io/thatsmypurse/utilitywatch-backend
          - name: frontend
            context: frontend
            image: docker.io/thatsmypurse/utilitywatch-frontend

    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DH_USER }}
          password: ${{ secrets.DH_TOKEN }}
      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ matrix.image }}
          tags: |
            type=sha
            type=ref,event=branch
            type=raw,value=v1
            type=raw,value=latest,enable={{is_default_branch}}
      - uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          push: true
          pull: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup SSH Key
      uses: webfactory/ssh-agent@v0.5.0
      with:
        ssh-private-key: ${{ secrets.DEPLOY_KEY }}


    - name: Deploy code
      uses: appleboy/ssh-action@master
      with:
      host: ${{ secrets.DEPLOY_HOST }}
      username: ${{ secrets.DEPLOY_USER }}
      key: ${{ secrets.DEPLOY_SSH_KEY }}
      script: |
        cd /apps/utilitwatch
        run ./deploy.sh
#    steps:
#        - name: "Configure SSH"
#          env:
#            SSH_USER: ${{ secrets.DEPLOY_USER }}
#            SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
#            SSH_HOST: ${{ secrets.DEPLOY_HOST }}
#            SSH_PORT: ${{ secrets.DEPLOY_PORT }}
#          run: |
#            mkdir -p ~/.ssh/
#            echo "$DEPLOY_SSH_KEY" > ~/.ssh/external-machine.key
#            chmod 600 ~/.ssh/external-machine.key
#            cat >>~/.ssh/config <<END
#            Host external-machine
#              HostName $DEPLOY_HOST
#              User $DEPLOY_USER
#              Port $DEPLOY_PORT
#              IdentityFile ~/.ssh/external-machine.key
#              StrictHostKeyChecking no
#            END
#
#        - name: Deploy content
#          run: ssh external-machine 'cd /var/www/yourcoderepository.com/build/ && ./deploy-content.sh'
##
#
#      - name: Setup SSH Key
#        uses: webfactory/ssh-agent@v0.5.0
#        with:
#          ssh-private-key: ${{ secrets.DEPLOY_KEY }}
#
#      - name: Deploy code
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{secrets.DEPLOY_HOST }}
#          username: ${{ secrets.DEPLOY_USER }}
#          key: ${{ secrets.DEPLOY_SSH_KEY }}
#          script: |
#            set -euo pipefail
#            cd ~/apps/utilitywatch
#            run ./deploy.sh
